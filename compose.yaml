services:
  boothdotdev:
    container_name: booth.dev
    pull_policy: build
    build:
      context: .
      dockerfile: BoothDotDev/Dockerfile
    environment:
      - DB_CONNECTION_STRING=${DB_CONNECTION_STRING}
    volumes:
      - /var/log/boothdotdev/site:/app/logs
      - /etc/boothdotdev/site:/app/data
    ports:
      - "2845:8080"
    restart: unless-stopped

networks:
  default:
    driver: bridge
